package org.example.sciencecalc.physics;

public enum ThermalConductivityUnit {
    WATTS_PER_METER_PER_KELVIN, // W/(m·K) = (kg·m²/s³) / (m·K) = kg·m / (s³·K)
    BRITISH_THERMAL_UNITS_PER_HOUR_FEET_FAHRENHEIT, // BTU/(h·ft²·°F)
    WATTS_PER_METER_SQUARED, // W/m²
    BRITISH_THERMAL_UNITS_PER_HOUR_FEET_SQUARED // BTU/(h·ft²)
}
